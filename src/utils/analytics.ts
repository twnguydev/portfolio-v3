/* eslint-disable */

export const trackPageView = () => {
  // Intégration avec Google Analytics, Plausible, ou autre
}

export const trackScrollDepth = (depth: number) => {
  // Tracking de la profondeur de lecture
}

export const trackEngagement = (type: 'like' | 'dislike') => {
  // Tracking des interactions
}